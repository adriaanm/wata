{
  "extends": "@react-native/typescript-config",
  "compilerOptions": {
    // Only include types needed for React Native, not Node.js
    "types": ["react-native"]
  },
  "include": ["src/**/*.ts", "src/**/*.tsx", "App.tsx", "index.js"],
  "exclude": ["**/node_modules", "**/Pods", "test/**/*"]
}
