{"key": "IOS-SIM-ASSEMBLY", "title": "the simulator-first leg of the iOS client, split out of IOS-CLIENT-ASSEMBLY per owner 2026-08-09 (start in simulator to flush the bugs that can be flushed there; developer-program signup is underway for the hardware legs). Scope: plan doc first, then a `uikit` bindgen target, a UIKit interpreter beside wata-mac's MacStage, wata-mac's bodies on it, and a simulator smoke modelled on mac-smoke (simctl MUST use the custom device set ~/.wata-simdevices \u2014 this Mac's ~/Library/Developer symlink breaks the default set; tools/ios-spike/spike.py is the working template). Architecture (A) per the spike: one pure-Go binary owning UIApplicationMain. Accepts the spike's argued-low risk that the signed-device-build question (IOS-HELLO-ON-PHONE) later vetoes (A) \u2014 the owner took that trade knowingly", "doc": "docs/plans/0044-ios-sim-assembly.md", "inProgress": true}
{"key": "ADULT-UX-NONHAPPY", "title": "owner 2026-08-09: real design work fleshing out the mac and iOS apps from an ADULT's usability perspective, and making sure the non-happy paths are enumerated, implemented, and tested — login/auth failures, server unreachable vs slow, clock-unset (the device's 1970 lesson applies to clients too), empty states, mid-session network loss, message-send failure surfaces, recovery without restart. Design work: starts as a plan doc that inventories today's error surfaces in wata-mac (and wataclient's states) against what an adult expects, then lands as implementation + tests per surface. Standing work — pick up whenever nothing above it is workable", "doc": "docs/plans/0003-parity-and-beyond.md", "inProgress": false}
{"key": "CI-ASSERT-CROSSING-INVENTORY", "title": "when CROSSING-INVENTORY-EMPTY-FILE lands upstream (a clean module writes an explicit .sgo/crossings-*.txt with total=0): repin, then add the consumer-side ratchet sgola suggested — a ci line asserting the inventory EXISTS and reads total=0 for each module, so 'checker silently off' can never again look identical to 'clean'", "doc": "docs/design/sgola-ffi.md", "inProgress": false, "blocked": "upstream landing notice for CROSSING-INVENTORY-EMPTY-FILE — nothing else; the recipe change is minutes once inventories exist"}
{"key": "PUREGO-STABLE", "title": "move the purego pin from v0.11.0-alpha.8 to v0.11.0 when it ships — an alpha is not where a shipping client should sit. Pure version bump; the gate that covered the alpha covers it", "doc": "docs/design/bindgen.md", "inProgress": false, "blocked": "the upstream v0.11.0 release existing (latest is v0.10.2, checked 2026-08-09) — nothing else; the bump itself is one line + the standard gate"}
{"key": "IOS-HELLO-ON-PHONE", "title": "run the EXISTING ios-spike hello on a real iPhone — the binary, bundling, and harness are done and simulator-green; the sole open question is dlopen + synthesized-class IMPs under a SIGNED DEVICE build, and it gates believing any (A)-based code ships (UI and audio both). A free personal-team profile suffices for this — the paid program is only for the PTT capability", "doc": "tools/ios-spike/REPORT.md", "inProgress": false, "blocked": "a physical iPhone in hand (owner) — nothing else; signing needs only a free personal team"}
{"key": "PTT-HELLO-HARDWARE", "title": "run the PushToTalk hello on a phone (plan 0026): system PTT UI up, join, transmit, the audio-session handoff logged — then AVAudioEngine on the allowlist for the actual mic round-trip", "doc": "tools/bindgen/hello/README.md", "inProgress": false, "blocked": "a physical iPhone + the paid Apple Developer Program membership (owner is enrolling as of 2026-08-09) + WATA_TEAM_ID — the PTT capability on an App ID is the part that needs the paid tier"}
{"key": "FB-EXIT-MENU-ON-DEVICE", "title": "plan 0040's handset leg — ONLY the finger work remains: open the exit menu on the handset, take `Restart app` and `Reboot` for real, walk fastboot/EDL as far as the SECOND prompt without confirming. Everything testable without hardware is done and pinned (PANEL-FIT golden + `wata-fb exitfit` in the fb smoke); the installed binary has the menu (2026-08-08 10:49)", "doc": "docs/plans/0040-exit-menu.md", "inProgress": false, "blocked": "the owner's fingers on the handset — no remote path exists (no /dev/uinput; the app reads only the three real keypads event0..2)"}
{"key": "CHIRP-DROP-PAGE-DURATION", "title": "drop the now-inert `-page_duration 20000` flag from tools/make-chirp.py at the next chirp regeneration (Ogg.readFrames walks the lacing table now, so the flag changes nothing) — but the asset is committed as BYTES and `--check` must keep reproducing them, so the flag comes out only when the chirp is re-encoded from source", "doc": "docs/plans/0039-startup-chirp.md", "inProgress": false, "blocked": "the owner's source recording (exists only on the owner's machine) — nothing else; the edit is one line + --check"}
{"key": "FB-BOOT-LOGO", "title": "boot polish pass, owner-deferred 2026-08-05 (not urgent). Two independent halves when it revives: (1) remaining console scroll at boot — an openrc/rc_quiet or console-redirect change in bq268-alpine, needing only a spec handoff to that repo, NOT hardware-gated; (2) the aboot splash frame — gated on an EDL flash session. Half (1) is workable the day the owner un-defers", "doc": "docs/plans/0003-parity-and-beyond.md", "inProgress": false, "blocked": "owner deferral (both halves); half 2 additionally needs EDL"}
{"key": "NET-WATCHDOG-SUPERVISION", "title": "the M4 flip test leaning on net-watchdog failover. The supervise-daemon fix is in the bq268-alpine repo (69a28b9) but NOT on the device", "doc": "docs/plans/0013-iroh-e2e.md", "inProgress": false, "blocked": "the next rootfs deploy/reboot with the owner present (reboot can stall in fastboot needing hands) → then bq268-alpine's on-device kill/respawn acceptance → only then our M4 flip test"}
{"key": "ESIM-PROVISION", "title": "the whole eSIM provisioning flow — carrier/profile choice, download/install onto a just-unboxed handset, onboarding implications. A bear; do not lose it", "doc": "docs/design/wata-fb.md", "inProgress": false, "blocked": "owner ruling 2026-08-06: parked until the wifi/cellular-fallback transport story is field-proven (WIFI-OFF-CMD is the test rig)"}
