# In-link libraries above the implicit prelude (core). wata-ios links json +
# wataclient (order: json then wataclient, matching wata-fb's chain) +
# wataui, whose differ drives the retained UIKit backend.
json
wataclient
wataui
