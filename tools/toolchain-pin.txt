# The sgola toolchain commit this repo builds against.
#
# Wata is a downstream consumer of sgola (the restricted-Scala-3-to-Go
# compiler). A local sgola checkout may be an active development tree; wata builds against
# a *pinned clone* instead, so a toolchain change is a deliberate, reviewable
# commit here rather than an invisible drift.
#
# Bump with:  tools/toolchain.py pin <sha>   (then re-run tools/toolchain.py sync)
#
repo   https://github.com/adriaanm/sgola.git
commit b4581b0f31d139c2a5a0b2765e510621f0ee8baa
