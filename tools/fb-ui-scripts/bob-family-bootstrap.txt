# bob: bootstrap phase — create `#family:localhost` with alice invited, and
# come up JOINED (the creator joins at create). This phase exists so a LATER
# alice phase sees bob as a joined family member: the roster's roomless DM
# rows are derived from the family's JOINED membership, so somebody has to be
# in the room before anyone can select their DM row. No checkpoints — the
# phase's product is server state. Shared by the dm-roundtrip and
# badges-across-restart scenarios.
#
# Driven by `wata-fb uitest` (uiscript.scala) — see that file's header for the
# directive vocabulary.

family @alice:localhost

wait syncing 1 900
wait convs 1 900
