# family-three, phase 1: the server minted the family room at boot with all
# three accounts joined (charlie exists because this scenario's server boots
# from a three-user $WATA_USERS file); alice sends the first clip into it.
# No checkpoints — later phases pin the pixels.

wait syncing 1 900
wait convs 1 900

# PTT into the selected (family) conversation
key ptt press
advance 4
key ptt release
wait msgs 1 1500
